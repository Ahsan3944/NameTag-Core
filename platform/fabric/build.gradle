plugins {
    id 'net.fabricmc.fabric-loom-remap' version '1.14.10'
}

dependencies {
    minecraft "com.mojang:minecraft:" + minecraft_version
    mappings "net.fabricmc:yarn:1.21.11+build.6:v2"
    modImplementation "net.fabricmc:fabric-loader:0.18.1"
    modImplementation "net.fabricmc.fabric-api:fabric-api:0.141.3+1.21.11"
    modImplementation "net.fabricmc.fabric-api:fabric-gametest-api-v1:3.1.27+4fc5413f3e"
    implementation project(':api')
    implementation project(':common')
    implementation project(path: ':versions:fabric-1.21.11', configuration: 'namedElements')
}

fabricApi {
    configureTests {
        createSourceSet = true
        modId = "nametag-core-test"
        enableGameTests = true
        enableClientGameTests = false
        eula = true
    }
}

dependencies {
    add("gametestImplementation", "net.fabricmc.fabric-api:fabric-gametest-api-v1:3.1.27+4fc5413f3e")
}

sourceSets.named("gametest") {
    compileClasspath += sourceSets.main.compileClasspath
    compileClasspath += sourceSets.main.output
    runtimeClasspath += sourceSets.main.runtimeClasspath
    runtimeClasspath += sourceSets.main.output
    runtimeClasspath += sourceSets.main.runtimeClasspath
}


tasks.named("compileGametestJava") {
    doFirst {
        logger.lifecycle("GAMETEST_COMPILE_CLASSPATH=" + sourceSets.gametest.compileClasspath.files.collect { it.name }.sort().join(","))
        logger.lifecycle("MAIN_RUNTIME_CLASSPATH=" + sourceSets.main.runtimeClasspath.files.collect { it.name }.sort().join(","))
        def matches = sourceSets.gametest.compileClasspath.files.findAll { file -> file.isFile() && file.name.endsWith(".jar") && zipTree(file).matching { include("**/GameTestHelper.class") }.files }
        logger.lifecycle("GAMETEST_HELPER_JARS=" + matches.collect { it.name }.join(","))
    }
}

tasks.withType(JavaCompile).configureEach { options.release = 21 }
